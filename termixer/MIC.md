- change the M label on the Master pane to MASTER.
- split the area within where the L/R peak bars and master gain slider are.
- mirror on the other side another gain slider, L/R peak
- below, where M and OUT buttons are...
  - stack the buttons M on bottom, OUT on top
  - in a new column to the right, put another two-button stack with IN on top (INPUT) and   on the bottom.

- setup a new audio channel to mix in microphone input.
- always start the TUI with mic muted (and instead of the MUTED label in the bottom right, let's do a   nerdfont glyph that toggles to  when unmuted— same inside the mic channel button
- input selection should be similar to output, but query for input devices
